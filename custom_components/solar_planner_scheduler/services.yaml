reset_to_auto:
  name: Reset to auto
  description: Clear a forced start time and let the coordinator search for the best slot again.
  fields:
    entity_id:
      required: true
      example: datetime.lave_linge_eco_coton_start
      selector:
        entity:
          domain: datetime
          integration: solar_planner_scheduler
